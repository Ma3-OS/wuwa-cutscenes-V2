use std::fs::File;
use std::io::{Read, Seek, SeekFrom};

fn main() -> std::io::Result<()> {
    // Open a known wuwa pak to check the exact payload offset
    let pak_path = "C:\\Games\\Wuthering Waves\\Wuthering Waves Game\\Client\\Content\\Paks\\pakchunk29-WindowsNoEditor.pak"; // Wait, I don't know the exact path.
    Ok(())
}
